[project]
name = "faiss-to-projector"
version = "0.1.0"
description = "Convert FAISS index to TensorFlow Projector format"
requires-python = ">=3.10"
dependencies = [
    "faiss-cpu>=1.7.4",
    "numpy>=1.24.0",
    "langchain-core>=0.1.0",
    "langchain-community>=0.0.10",
]
